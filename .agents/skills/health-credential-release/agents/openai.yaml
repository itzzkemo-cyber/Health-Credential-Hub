interface:
  display_name: "Health Credential Release"
  short_description: "Prepare the production web release for handoff"
  default_prompt: "Use $health-credential-release to prepare this site for a verified production release handoff."
