{
  "name": "health-credential-hub",
  "version": "1.0.0",
  "description": "Production-focused healthcare workforce credential management platform with a responsive web application and secure API.",
  "license": "MIT",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11.19.0 <12"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wathaiqihealth/Health-Credential-Hub.git"
  },
  "scripts": {
    "preinstall": "node -e \"const fs=require('node:fs'); for (const file of ['package-lock.json','yarn.lock']) fs.rmSync(file,{force:true}); if (!process.env.npm_config_user_agent?.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:production": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/health-docs run build:production",
    "db:migrate": "pnpm --filter @workspace/db run migrate",
    "start:production": "pnpm --filter @workspace/api-server run start",
    "test": "pnpm run test:operations && pnpm -r --if-present run test",
    "test:operations": "node --test scripts/check-supabase-storage.test.mjs scripts/request-uptimerobot-monitor.test.mjs scripts/operations/backup.test.mjs scripts/operations/backup-postgres.test.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~7.0.2"
  }
}
