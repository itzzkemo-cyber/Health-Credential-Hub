# Runtime
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# PostgreSQL
DATABASE_URL=

# Generate a unique random value of at least 32 characters for production.
SESSION_SECRET=
# Comma-separated HTTPS origins allowed to call the API (custom domain and
# any separate mobile/web origin). Same-origin requests work automatically.
APP_ORIGINS=
# Canonical deployed URL used for OAuth callbacks, email links, and QR links.
PUBLIC_APP_URL=
# Production defaults to Lax. Use none only for a trusted cross-site preview.
SESSION_COOKIE_SAME_SITE=lax
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
TOTP_ENCRYPTION_KEY=

# Demo and account provisioning (production-safe defaults)
DEMO_LOGIN_ENABLED=false
ALLOW_DEMO_SEED=false
SELF_REGISTRATION_ENABLED=false
GOOGLE_AUTO_PROVISION_ENABLED=false

# Object storage
GOOGLE_CLOUD_PROJECT=
# Format: /bucket-name/private (the bucket must remain private)
PRIVATE_OBJECT_DIR=
# Optional public asset roots, comma-separated: /bucket-name/public
PUBLIC_OBJECT_SEARCH_PATHS=
# Optional data-sovereignty endpoint, e.g. https://storage.me-central2.rep.googleapis.com
STORAGE_API_ENDPOINT=

# Gemini OCR (optional; only required when OCR is used)
AI_INTEGRATIONS_GEMINI_BASE_URL=
AI_INTEGRATIONS_GEMINI_API_KEY=

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email alerts (disabled safely until a provider is provisioned)
EMAIL_ALERTS_DISABLED=1
EMAIL_FROM=
RESEND_API_KEY=

# Web/mobile build configuration
BASE_PATH=/health-docs/
EXPO_PUBLIC_API_URL=http://localhost:3000
EXPO_PUBLIC_WEB_URL=http://localhost:4173/health-docs
EXPO_PUBLIC_DOMAIN=
